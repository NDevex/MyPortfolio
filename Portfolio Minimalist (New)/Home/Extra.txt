

@media (min-width: 320px) {

    /* GENERAL */
    body {
        background-color: #121212;
    }

    /* NAVBAR */
   .navbar {
        display: flex;
        gap: 100px;
        list-style: none;
        font-weight: 100;
        font-size: 40px;
        font-family: "Open Sans", sans-serif;
        font-optical-sizing: auto;
        font-weight: 10;
        font-style: normal;
        position: absolute;
        top: 50px;
        right: 100px;
}


/* HOME LABEL */
.HomeLabel {
    position: relative;
    color: #ffff;
    text-decoration: none;
    display: inline-block;
}

.HomeLabel::after {
    content: "";
    position: absolute;
    bottom: -3px;
    left: 50%;
    width: 0px;
    transform: translateX(-50%) scaleX(0);
    height: 1px;
    background-color: white;
    border-radius: 1px;
    transition: 0.3s ease-in-out;
}

.HomeLabel:hover::after {
    width: 30px;
    transform: translateX(-50%);
    height: 1px;
    background-color: white;
    border-radius: 1px;
}

/* ABOUT ME LABEL */
.AboutMeLabel {
    position: relative;
    color: #ffff;
    text-decoration: none;
    display: inline-block;
}

.AboutMeLabel::after {
    content: "";
    position: absolute;
    bottom: -3px;
    left: 50%;
    width: 0px;
    transform: translateX(-50%) scaleX(0);
    height: 1px;
    background-color: white;
    border-radius: 1px;
    transition: 0.3s ease-in-out;
}

.AboutMeLabel:hover::after {
    width: 30px;
    transform: translateX(-50%);
    height: 1px;
    background-color: white;
    border-radius: 1px;
}

.navbar :hover {
    color: #ccc5b9;
    transform: scale(1);
    transition: 0.5s ease-in-out;
}


    /* FORM */
    .Placement {
        display: grid;
        gap: 20px;
        position: absolute;
        left: 50%;
        top: 350px;
        transform: translateX(-50%);
    }

    /* PLACEHOLDER */
    .Placement input::placeholder,
    .MessageBox::placeholder {
        color: #ccc5b9;
    }

    /* INPUTS */
    .Placement input,
    .MessageBox {
        color: #f5f5f5;
        background-color: #403d39;
        text-decoration: none;
        box-sizing: border-box;
        padding: 20px;
        width: 600px;
        height: 70px;
        border: #121212;
        border-radius: 20px;
        font-family: "Open Sans", sans-serif;
        outline: none;
    }

    /* INPUT FOCUS */
    .Placement input:focus,
    .Placement input:not(:placeholder-shown),
    .MessageBox:focus,
    .MessageBox:not(:placeholder-shown) {
        background-color: #403d39;
        color: #f5f5f5;
    }

    /* AUTOFILL */
    .Placement input:-webkit-autofill,
    .Placement input:-webkit-autofill:hover,
    .Placement input:-webkit-autofill:focus {
        -webkit-box-shadow: 0 0 0 1000px #403d39 inset;
        -webkit-text-fill-color: #f5f5f5;
        caret-color: #f5f5f5;
    }

    /* MESSAGE BOX */
    .MessageBox {
        height: 95px;
        overflow-y: auto;
        resize: none;
        padding-bottom: 20px;
    }

    /* SCROLLBAR */
    .MessageBox::-webkit-scrollbar {
        width: 3px;
    }

    .MessageBox::-webkit-scrollbar-button {
        display: none;
        width: 0;
        height: 0;
    }

    .MessageBox::-webkit-scrollbar-track {
        background: transparent;
        border: none;
    }

    .MessageBox::-webkit-scrollbar-thumb {
        background-color: #3A3A3A;
        border: none;
        border-radius: 10px;
        width: 2px;
    }

    .MessageBox {
        scrollbar-color: #3A3A3A transparent;
        scrollbar-width: thin;
    }

    /* BUTTON */
    .CButton {
        position: absolute;
        left: 50%;
        top: 700px;
        transform: translateX(-50%);
        background-color: #f5f5f5;
        color: black;
        font-family: "Open Sans", sans-serif;
        font-size: medium;
        font-weight: 100;
        text-decoration: none;
        box-sizing: border-box;
        width: 200px;
        height: 60px;
        border: none;
        border-radius: 50px;
        transition: 0.3s ease-in-out;
    }

    .CButton:hover {
        background-color: #3A3A3A;
        color: #f5f5f5;
        cursor: pointer;
    }

    /* SOCIAL APPS */
    .Apps {
        font-size: xx-large;
        display: flex;
        position: absolute;
        right: 200px;
        bottom: 150px;
        gap: 10px;
        list-style: none;
        text-decoration: none;
        background-clip: text;
    }

    .Apps a {
        color: #f5f5f5;
        text-decoration: none;
        transition: 0.5s ease-in-out;
    }

    .Apps a:hover {
        rotate: 10deg;
        scale: 1.2;
        transition: 0.3s ease-in-out;
        color: grey;
        filter: brightness(1.5);
    }

}


/* GENERAL */
@media (min-width: 1200px) {
    body {
        background-color: #121212;
    }
}

/* NAVBAR */
.navbar {
    display: flex;
    gap: 100px;
    list-style: none;
    font-weight: 100;
    font-size: 20px;
    font-family: "Open Sans", sans-serif;
    font-optical-sizing: auto;
    font-weight: 10;
    font-style: normal;
    position: absolute;
    top: 50px;
    right: 200px;
}

.navbar :hover {
    color: #ccc5b9;
    transform: scale(1);
    transition: 0.5s ease-in-out;
}

/* HOME LABEL */
.HomeLabel {
    position: relative;
    color: #ffff;
    text-decoration: none;
    display: inline-block;
}

.HomeLabel::after {
    content: "";
    position: absolute;
    bottom: -3px;
    left: 50%;
    width: 0px;
    transform: translateX(-50%) scaleX(0);
    height: 1px;
    background-color: white;
    border-radius: 1px;
    transition: 0.3s ease-in-out;
}

.HomeLabel:hover::after {
    width: 30px;
    transform: translateX(-50%);
    height: 1px;
    background-color: white;
    border-radius: 1px;
}

/* ABOUT ME LABEL */
.AboutMeLabel {
    position: relative;
    color: #ffff;
    text-decoration: none;
    display: inline-block;
}

.AboutMeLabel::after {
    content: "";
    position: absolute;
    bottom: -3px;
    left: 50%;
    width: 0px;
    transform: translateX(-50%) scaleX(0);
    height: 1px;
    background-color: white;
    border-radius: 1px;
    transition: 0.3s ease-in-out;
}

.AboutMeLabel:hover::after {
    width: 30px;
    transform: translateX(-50%);
    height: 1px;
    background-color: white;
    border-radius: 1px;
}

/* HEADER */
.ConnectHeader {
    color: #f5f5f5;
    font-family: "Arimo", sans-serif;
    font-optical-sizing: auto;
    font-weight: 10;
    text-align: center;
    position: relative;
    top: 150px;
}

/* SENTENCE */
.Sentence {
    color: #e0e0e0;
    font-family: "Open Sans", sans-serif;
    font-optical-sizing: auto;
    font-weight: 10;
    font-style: normal;
    font-size: 20px;
    text-align: center;
    position: relative;
    top: 200px;
}

/* FORM */
.Placement {
    display: grid;
    gap: 20px;
    position: absolute;
    left: 50%;
    top: 350px;
    transform: translateX(-50%);
}

/* PLACEHOLDER */
.Placement input::placeholder,
.MessageBox::placeholder {
    color: #ccc5b9;
}

/* INPUTS */
.Placement input,
.MessageBox {
    color: #f5f5f5;
    background-color: #403d39;
    text-decoration: none;
    box-sizing: border-box;
    padding: 20px;
    width: 600px;
    height: 70px;
    border: #121212;
    border-radius: 20px;
    font-family: "Open Sans", sans-serif;
    outline: none;
}

/* INPUT FOCUS */
.Placement input:focus,
.Placement input:not(:placeholder-shown),
.MessageBox:focus,
.MessageBox:not(:placeholder-shown) {
    background-color: #403d39;
    color: #f5f5f5;
}

/* AUTOFILL */
.Placement input:-webkit-autofill,
.Placement input:-webkit-autofill:hover,
.Placement input:-webkit-autofill:focus {
    -webkit-box-shadow: 0 0 0 1000px #403d39 inset;
    -webkit-text-fill-color: #f5f5f5;
    caret-color: #f5f5f5;
}

/* MESSAGE BOX */
.MessageBox {
    height: 95px;
    overflow-y: auto;
    resize: none;
    padding-bottom: 20px;
}

/* SCROLLBAR */
.MessageBox::-webkit-scrollbar {
    width: 3px;
}

.MessageBox::-webkit-scrollbar-button {
    display: none;
    width: 0;
    height: 0;
}

.MessageBox::-webkit-scrollbar-track {
    background: transparent;
    border: none;
}

.MessageBox::-webkit-scrollbar-thumb {
    background-color: #3A3A3A;
    border: none;
    border-radius: 10px;
    width: 2px;
}

.MessageBox {
    scrollbar-color: #3A3A3A transparent;
    scrollbar-width: thin;
}

/* BUTTON */
.CButton {
    position: absolute;
    left: 50%;
    top: 700px;
    transform: translateX(-50%);
    background-color: #f5f5f5;
    color: black;
    font-family: "Open Sans", sans-serif;
    font-size: medium;
    font-weight: 100;
    text-decoration: none;
    box-sizing: border-box;
    width: 200px;
    height: 60px;
    border: none;
    border-radius: 50px;
    transition: 0.3s ease-in-out;
}

.CButton:hover {
    background-color: #3A3A3A;
    color: #f5f5f5;
    cursor: pointer;
}

/* SOCIAL APPS */
.Apps {
    font-size: xx-large;
    display: flex;
    position: absolute;
    right: 200px;
    bottom: 150px;
    gap: 10px;
    list-style: none;
    text-decoration: none;
    background-clip: text;
}

.Apps a {
    color: #f5f5f5;
    text-decoration: none;
    transition: 0.5s ease-in-out;
}

.Apps a:hover {
    rotate: 10deg;
    scale: 1.2;
    transition: 0.3s ease-in-out;
    color: grey;
    filter: brightness(1.5);
}

@media (min-width: 320px) and (max-width: 1199px) {
    .navbar {
        display: flex;
        gap: 100px;
        list-style: none;
        font-weight: 10;
        font-size: 40px;
        font-family: "Open Sans", sans-serif;
        font-optical-sizing: auto;
        font-style: normal;
        position: absolute;
        top: 50px;
        right: 100px;
    }

        /* HEADER */
    .ConnectHeader {
        color: #f5f5f5;
        font-family: "Arimo", sans-serif;
        font-optical-sizing: auto;
        font-weight: 10;
        text-align: center;
        position: relative;
        top: 350px;
    }

    /* SENTENCE */
    .Sentence {
        color: #e0e0e0;
        font-family: "Open Sans", sans-serif;
        font-optical-sizing: auto;
        font-weight: 10;
        font-style: normal;
        font-size: 20px;
        text-align: center;
        position: relative;
        top: 500px;
    }
}
